<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Giới tính</title>
    <%- include('../_share/style'); %>
</head>
<body>
<div class="container-fluid">

    <main class="container">
        <%- include('../_share/header'); %>
        <h4 class="text-center">Giới tính</h4>
        <table class="table table-striped">
            <thead>
                <th>ID</th>
                <th>Giới tính</th>
            </thead>

            <tbody>
            <% for(let i = 0; i < genders.length; i++) {%>
                <tr>
                    <td><%= genders[i].id %></td>
                    <td><%= genders[i].name %></td>
                </tr>
            <% } %>
            </tbody>
        </table>
    </main>
</div>
</body>
</html>

