<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thông tin người dùng</title>
    <%- include('../_share/style'); %>
</head>
<body>
<div class="container">
    <%- include('../_share/header'); %>

    <main class="container-fluid">
        <div class="text-center" style="margin-bottom: 20px;">
            <% if(!user.avatarUrl){ %>
                <img src="https://vectorified.com/images/no-image-icon-3.png"
                     style="max-width: 200px;"
                     class="userAvatar"
                />
            <% } else{ %>
                <img src="<%= user.avatarUrl %>"
                     style="max-width: 200px;"
                     class="userAvatar"
                />
            <% } %>

            <h4 style="text-transform: uppercase;">
                <%= user.fullName %>
            </h4>
        </div>

        <div style="margin-bottom: 25px;">
            <p><strong>Mức lương mong muốn</strong></p>

            <% if(!user.expectedSalaryInMilionVnd){ %>
                <p>Chưa cập nhật</p>
            <% } else{ %>
                <p><%= user.expectedSalaryInMilionVnd %> triệu trở lên</p>
            <% } %>
        </div>

        <div style="margin-bottom: 25px;">
            <p><strong>Loại hình công việc</strong></p>

            <% if(!user.typeOfWork){ %>
                <p>Chưa cập nhật</p>
            <% } else{ %>
                <p><%= user.typeOfWork %></p>
            <% } %>
        </div>

        <div style="margin-bottom: 25px;">
            <p><strong>Trình độ học vấn</strong></p>

            <% if(!user.graduatedEducation){ %>
                <p>Chưa cập nhật</p>
            <% } else{ %>
                <p><%= user.graduatedEducation %></p>
            <% } %>
        </div>

        <div style="margin-bottom: 25px;">
            <p><strong>Kinh nghiệm làm việc</strong></p>

            <% if(!user.yearsOfExperience){ %>
                <p>Chưa cập nhật</p>
            <% } else{ %>
                <p><%= user.yearsOfExperience %> năm</p>
            <% } %>
        </div>

        <div style="margin-bottom: 25px;">
            <p style="text-transform: uppercase">
                <strong>MÔ TẢ BẢN THÂN</strong>
            </p>

            <% if(!user.resumeSummary) { %>
                <p>Chưa cập nhật</p>
            <% } else{ %>
                <p><%= user.resumeSummary %></p>
            <% } %>
        </div>

        <div style="margin-bottom: 25px;">
            <p style="text-transform: uppercase">
                <strong>MỤC TIÊU NGHỀ NGHIỆP</strong>
            </p>

            <p><%= user.careerObjective %></p>

            <% if(!user.careerObjective) { %>
                <p>Chưa cập nhật</p>
            <% } else{ %>
                <p><%= user.careerObjective %></p>
            <% } %>
        </div>

        <div style="margin-bottom: 25px;">
            <p style="text-transform: uppercase">
                <strong>LIÊN HỆ</strong>
            </p>
            <table>
                <tr>
                    <td width="100px"><strong>Ngày sinh</strong></td>
                    <td>
                        <% if(!user.birthdayInMiliseconds){ %>
                            <span>Chưa cập nhật</span>
                        <% } else{ %>
                            <span>
                                <%= user.birthdayInMiliseconds %>
                            </span>
                        <% } %>
                    </td>
                </tr>

                <tr>
                    <td><strong>Địa chỉ</strong></td>

                    <% if(!user.address){ %>
                        <td>Chưa cập nhật</td>
                    <% } else{ %>
                        <td><%= user.address %></td>
                    <% } %>
                </tr>

                <tr>
                    <td><strong>SĐT</strong></td>
                    <td><%= user.phone %></td>
                </tr>

                <tr>
                    <td><strong>Email</strong></td>
                    <td><%= user.email %></td>
                </tr>
            </table>
        </div>

    </main>

    <%- include('../_share/footer'); %>
</div>
</body>
</html>